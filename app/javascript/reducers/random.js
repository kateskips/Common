const randomReducer = (state = null, action) => {
    switch (action.type) {
        case "RANDOMIZER":
            return state
        default:
            return state
    }
}

export default randomReducer